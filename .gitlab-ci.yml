image: rocker/verse

pages:
  stage: deploy
  script:
      - Rscript -e "packages.install('ggthemes')"
      - Rscript -e "bookdown::render_book('index.Rmd', 'all', output_dir = 'public')"
  artifacts:
    paths:
        - public
  only:
      - master
